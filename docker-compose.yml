
version: '3'

services:
  db: 
    image: mysql
    ports:
       - "3306:3306"
    volumes:
        - db_data:/var/lib/mysql
  web:
    build: .
    command: python -m flask run --host=0.0.0.0
    volumes:
      - .:/code
    ports:
      - "5000:5000"
    depends_on:
      - db
